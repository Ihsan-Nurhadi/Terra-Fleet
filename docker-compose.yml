services:
  fleet-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nayaka_fleet_monitoring
    restart: unless-stopped
    ports:
      # Map host VPS port 3000 to Nginx container port 80
      - "3000:80"
    environment:
      - TZ=Asia/Jakarta
